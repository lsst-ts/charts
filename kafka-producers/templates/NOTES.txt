Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  helm status {{ .Release.Name }}
  helm get {{ .Release.Name }}

To check if all the Kafka Producers are running, try:

  kubectl get pods -l "app={{ include "kafka-producers.name" . }}"

To check logs for the individual Kafka Producers, try


{{- range $csc := list "auxtel" "comcam" "eas" "latiss" "m1m3" "m2" "obssys" "test" }}
{{- if (has $csc $.Values.producers ) }}
  kubectl logs $(kubectl get pods -l "app={{ $.Release.Name }}-{{ $csc }}" -o jsonpath="{.items[0].metadata.name}")
{{- end }}
{{- end -}}
