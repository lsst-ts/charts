apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "obssys-producers.name" . }}
  labels:
    helm.sh/chart: {{ include "kafka-producers.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "obssys-producers.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ include "obssys-producers.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: {{ include "obssys-producers.name" . }}
          {{- include "kafka-producers.container-boilerplate" . | nindent 10 }}
          env:
            - name: LSST_DDS_DOMAIN
              {{- with .Values.env.lsstDdsDomain }}
              value: {{ .obssys | default .default | quote }}
              {{- end }}
            {{- include "kafka-producers.env-boilerplate" . | nindent 12 }}
            - name: CSC_LIST 
              value: "GenericCamera Script ScriptQueue Watcher"

